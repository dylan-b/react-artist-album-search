import * as React from 'react';
import {
    Drawer
  } from '@material-ui/core';

  const Settings = () => {

    return (
        <div>
             <Drawer>
            </Drawer>
        </div>
    );
}

export default Settings;